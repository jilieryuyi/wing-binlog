<?php

return  [
    //必须配置：mysqlbinlog 命令，如果无法识别 写绝对路径
    "mysqlbinlog" => "mysqlbinlog",
    //必须配置：mysql
    "mysql" => [
        "db_name"  => "wordpress",
        "host"     => "127.0.0.1",
        "user"     => "root",
        "password" => "123456",
        "port"     => 3306
    ],
    //可选redis配置 当使用--with-redis 启用redis队列通知时必须要配置
    "redis" => [
        "host"     => "127.0.0.1",
        "port"     => 6397,
        "password" => null,                          //无密码时必须为null
        "queue"    => "----wing-mysql-events-queue----" //默认的redis队列名称，队列使用rpush从尾部进入队列
    ],
    //可选websocket配置 当使用--with-websocket 启用websocket服务通知时必须要配置
    "websocket" => [
        "host"  => "0.0.0.0",
        "port"  => 9998,
    ],
    //可选tcp配置 当使用--with-tcp 启用tcp服务通知时必须要配置
    "tcp" => [
        "host" => "0.0.0.0",
        "port" => 9997
    ]
];

